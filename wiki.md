---
title: InfoDot — Platform Wiki
version: 1.3.0
status: active
owners: [InfoDot Platform Lead]
platform-id: infodot
last-review: 2026-08-06
---

# InfoDot

Purpose: this is InfoDot's own knowledge home — owned and maintained by the InfoDot team. It describes what this platform actually is, as implemented, and how it connects to the wider Dot Ecosystem.

---

## 1. What InfoDot Is

InfoDot is the hub of the Dot Ecosystem — the central identity provider that lets a user log in once and move between every connected Dot platform without re-authenticating (`App\Http\Controllers\Auth\EcosystemAuthController` receives the handoff, `App\Http\Controllers\Api\EcosystemTokenController` / `App\Livewire\EcosystemWidget` issue it). It also carries its own community/knowledge-base product: a public Q&A section (`Questions`), a public Solutions/how-to hub (`Solutions` + `Steps`), threaded comments and polymorphic likes on both, a lightweight social graph (`Associates`), and a team-scoped "Team Drive" storage layer (`Obj`/`File`/`Folder`) that is modeled and migrated but has no controller wired to it yet. Laravel 12, PHP 8.4/8.5, Livewire 3, Jetstream 5 with Teams enabled, PostgreSQL.

## 2. Architecture

| Layer | Technology | Notes |
|---|---|---|
| Framework | Laravel 12, PHP 8.4+ | |
| UI | Livewire 3, DaisyUI 5 | |
| Database | PostgreSQL | `DB_DATABASE=infodot` per `.env.example`, shared-instance ecosystem convention |
| Auth | Sanctum + Jetstream Teams (`Features::teams(['invitations' => true])` in `config/jetstream.php`) | Teams feature is genuinely on, but only the storage layer (`objects`/`files`/`folders`) actually has a `team_id` column — the community content (questions/solutions/comments/likes/associates) is deliberately global, not team-scoped (see §"Tenant Scope Audit") |
| Search | Laravel Scout (TNTSearch driver) | `Obj` and presumably `Comment`/other searchable models index into it |

## 3. Tenant Scope Audit (this pass)

Before adding any scope trait, every model in `app/Models/*.php` was checked against its migration for a `user_id`/`team_id` column and against its actual controllers/Livewire components for how it's queried:

| Model | Owns | Scoped this pass? | Why |
|---|---|---|---|
| `Obj`, `File`, `Folder` | `team_id` (`objects`/`files`/`folders` tables) | **Yes** — `HasTeamScope` | Genuine per-team private storage ("Team Drive"). Not yet wired to any controller, but the column and intent are real, and scoping it now means the first controller ever written against it is safe by default instead of depending on someone remembering `->where('team_id', ...)`. `Obj` already carried a `RelatesToTeams` trait with a `scopeForCurrentTeam()` local scope — but it's opt-in (must be called explicitly) and, per a repo-wide grep, is never actually called anywhere. Left in place untouched; `HasTeamScope` supersedes it as the real, automatic guard. |
| `Questions`, `Solutions`, `Steps`, `Comment`, `Like`, `Associates` | `user_id` (authorship) | **No** | These are genuinely public, shared community content — a Q&A feed and a Solutions/how-to hub every authenticated user browses regardless of who authored a given row (`QuestionsController::index()`/`SolutionsController::index()` intentionally list *everyone's* posts). `user_id` here records authorship for display and for the author's own edit/delete affordances, not tenant isolation — scoping these globally to the current user would break the product (a user would only ever see their own questions). This is the "legitimately global/shared" carve-out the pattern calls for, analogous to Dot.Notify's `NotifyPreference` exclusion, just for a different reason (public content vs. genuinely per-user settings). `Associates` (a follow/connection edge with both a `user_id` and an `associate_id`) is also excluded: `Livewire\Associates::connect()` deliberately queries both directions of the relationship (who I follow, and separately elsewhere who follows me), so a blanket `user_id = Auth::id()` scope would hide edges the app needs to read from the other side. |
| `Team`, `TeamInvitation`, `User`, `Membership` | n/a (Jetstream core) | No | Framework scaffolding, out of scope. |

## 4. HasTeamScope

`app/Models/Concerns/HasTeamScope.php` — an Eloquent global scope, applied via `use HasTeamScope;` to `Obj`, `File`, `Folder`. Mirrors Dot.Notify's `HasTeamScope` / Dot.Finance's `HasUserScope` exactly: `bootHasTeamScope()` registers a global scope that adds `where('<table>.team_id', Auth::user()->currentTeam->id)` whenever `Auth::check()` and the user has a current team. No controller changes were needed this pass — no controller currently queries `Obj`/`File`/`Folder` at all — but the trait is now load-bearing groundwork for whenever that storage layer is actually built out.

## 5. Testing

`tests/Feature/InfoDotTeamScopeTest.php::test_scope_alone_blocks_cross_team_access_even_without_an_explicit_where` proves the scope alone (no Policy, no explicit `where`) blocks cross-team reads on `File`: creates a file owned by team A, logs in as a user on team B, asserts `File::find($id)` is null and `File::query()->count()` is 0, then logs in as the owner and asserts both are non-null/1.

Full suite verified against real PostgreSQL (fresh `infodot_pilot` database, migrated clean): 93 tests carry a pre-existing PHP 8.5 deprecation notice (`PDO::MYSQL_ATTR_SSL_CA` referenced in `config/database.php`, unrelated to this pass, not touched) plus 1 clean unit test — **0 failures**, both before and after this pass's changes (92 deprecated/1 passed baseline → 93 deprecated/1 passed after adding the new regression test).

## 6. Static Analysis & Dependency Audit (this pass)

- **PHPStan/Larastan** (`larastan/larastan` was already a `require-dev` dependency; `phpstan.neon` already existed at level 5 with the Larastan extension) — `vendor/bin/phpstan analyse --memory-limit=1G` **ran successfully in this sandbox** (unlike every other platform in this rollout) and reported **no errors** against `app/`, including the new `HasTeamScope` trait and its three consumers.
- **`composer audit`** found 6 pre-existing `guzzlehttp/guzzle` advisories (1 high — CVE-2026-69246 noncanonical-host check bypass — plus 5 medium). Fixed via a clean transitive bump: `composer update guzzlehttp/guzzle guzzlehttp/psr7 guzzlehttp/promises --with-all-dependencies`. `composer.json`'s existing `^7.10` constraint already covered the patched versions, so only `composer.lock` changed. `composer audit` clean afterward; full test suite re-confirmed 0 failures post-update.

## Change Log

| Version | Date | Author | Change |
|---|---|---|---|
| 1.3.0 | 2026-08-06 | Platform-loop pass | **InfoDot as pilot for an ecosystem-wide standardization pass** (shared email templates, complete legal pages, auth pages matching the welcome hero) — built here first for sign-off before propagating to the other 25 Dot Ecosystem platforms. Three workstreams: <br>**(1) Branded email system** — published Laravel's Markdown mail theme (`resources/views/vendor/mail/`) and re-themed it to InfoDot's gold/charcoal brand: a dark charcoal header band carrying the real logo (`message.blade.php` now injects `<img>` instead of plain app-name text), gold pill buttons with charcoal text, ink/paper body tones. All colors are hardcoded hex, not `var()` — email clients (Outlook chief among them) don't reliably support CSS custom properties. This is genuinely shared/reusable: every Laravel notification that renders mail through `MailMessage` (Fortify's password reset and email verification, Jetstream's team invitations) picks up the theme automatically, zero per-notification code changes. Verified by rendering a real `ResetPassword` notification through a temporary local-only preview route (removed before shipping) at 1280px and 375px. <br>**(2) Complete legal pages** — Privacy Policy, Terms & Conditions, and a new Cookie Policy, aligned to South Africa's POPIA (Protection of Personal Information Act), naming BluePin Inc as the responsible party per the live infodot.co.za site's own footer. Adopted Jetstream's Markdown-source convention for real: content lives in `resources/markdown/{terms,policy,cookies}.md` (Jetstream's own stub placeholders, `Edit this file to define...`, were still sitting there unedited), rendered through thin Blade wrappers — editing the legal wording going forward is a one-file content change, no markup involved, and the same `.md` files can be copied verbatim into another platform's `resources/markdown/` as the "generate once, propagate copies" central-maintenance approach. Enabled Jetstream's `Features::termsAndPrivacyPolicy()` (was commented out) now that real content backs it, which surfaced and fixed two dormant bugs: `policy.blade.php` had no route or controller method at all (completely unreachable), and `register.blade.php`'s consent checkbox already referenced `route('terms.show')`/`route('policy.show')` — names that didn't exist yet. Also found and worked around a real Laravel routing footgun while wiring this up: registering two `Route::get()` calls at the identical URI with different names silently evicts the first from `RouteCollection`'s name lookup table (`allRoutes` is keyed by method+URI, last write wins; `refreshNameLookups()` only sees survivors) — throws `RouteNotFoundException` with no indication why. Landed on distinct route names (`terms`/`policy`/`cookies` for the app's own nicer URLs, leaving Jetstream's auto-registered `terms.show`/`policy.show` at `/terms-of-service`/`/privacy-policy` alone) rather than fighting the collision. Added a Cookie Policy grounded in the real cookies this app sets (`infodot_session`, `XSRF-TOKEN`, `remember_web_*` — verified against `config/session.php`, no analytics/ad trackers exist to describe). Rebuilt the shared `layouts/footer.blade.php` partial (used by 21 views across the app) while here: fixed a broken `#javascript-date` span that never actually rendered a year, a hardcoded `https://www.infodot.co.za` link instead of `url('/')`, re-toned it from unbranded gray to the ink/gold tokens, and added the new Privacy/Cookie links alongside Terms — one shared-component fix instead of touching 21 files individually. <br>**(3) Auth pages matching the welcome hero** — `authentication-card.blade.php` (the one shared component every guest auth page — login, register, forgot-password, reset-password, confirm-password, two-factor-challenge, verify-email — renders through) now carries the exact same full-bleed photo + graduated radial/linear scrim as `welcome.blade.php`'s hero, reusing the identical Unsplash URL rather than a second asset. `authentication-card-logo.blade.php` was simplified back to a plain logo (the 1.2.4 pass's dark charcoal chip is redundant once the whole page is dark). Legal/info pages (terms, policy, cookies, about, contact) deliberately keep their light `--paper` background — they use `x-guest-layout` directly, not `x-jet-authentication-card`, so this change only touches the seven true auth pages, not prose-heavy content pages where a dark photo backdrop would hurt readability. <br>Also fixed two long-standing pre-existing test failures flagged (not fixed) in 1.2.3/1.2.4's own changelog entries: `AuthenticationFlowSmokeTest`'s stale `'Sign in with credentials'` assertion (real copy is `'Sign in with your credentials'`), and its registration test not submitting a `terms` field, which started failing the moment `termsAndPrivacyPolicy` was enabled. Verified: `npm run build` clean; `php artisan test` **87/87 passing** (0 skipped-but-relevant failures, up from 86/87 with 1 known pre-existing failure in every prior pass this session); every changed page reloaded and screenshotted at 1280px and 375px (login, register, forgot-password, terms, policy, cookies, welcome); zero console errors; register page's consent checkbox links confirmed pointing at working routes via `read_page`. |
| 1.2.4 | 2026-08-06 | Platform-loop pass | Swapped the old blue "inf" logo (`img/logo.png`) for the new gold logo (`img/logo_white.png`) on every guest/auth page (login, register, forgot-password, reset-password, confirm-password, two-factor-challenge, verify-email, terms, policy) — the 1.2.2 pass had deliberately left these on the old logo because the new mark's white wordmark is illegible directly on the guest layout's light `--paper` background. Fixed properly this time instead of leaving it: `authentication-card-logo.blade.php` now wraps the new logo in a rounded `bg-[var(--charcoal)]` chip, giving it the same dark backdrop it already reads correctly against on the welcome page's hero/CTA/footer sections. Since the logo now carries the welcome page's gold identity, the rest of the guest-page chrome was re-tokened to match rather than leaving a gold logo sitting on an otherwise-blue page: `layouts/guest.blade.php`'s `:root` block dropped the old `--blue`/`--blue-deep`/`--silver` tokens (the last was defined but never actually used) for `--gold`/`--gold-deep`/`--charcoal`, matching the welcome page's exact hex values. Because every consumer (`button.blade.php`, `input.blade.php`, `checkbox.blade.php`, and the terms/privacy links on `login.blade.php`/`register.blade.php`/`terms.blade.php`/`policy.blade.php`) referenced these as CSS custom properties rather than hardcoded hex, most of the recolor cascaded from the one token change; `button.blade.php` additionally needed `text-white` → `text-[var(--charcoal)]` since white text on a light-gold button background is illegible. Caught and fixed the same `[var(--x)]/NN` Tailwind opacity-modifier bug documented elsewhere this session while touching `input.blade.php`/`checkbox.blade.php`'s focus rings (`focus:ring-[var(--blue)]/20`, silently non-functional) — replaced with precomputed `focus:ring-[rgba(240,188,46,0.2)]`, verified via a live `getComputedStyle` check that the ring now actually renders (`boxShadow` includes `rgba(240, 188, 46, 0.2) 0px 0px 0px 3px`). The `--gold-deep` link color chosen for terms/privacy/forgot-password links deliberately isn't used as the resting-state text color on white (computed contrast ≈2.3:1 against white, fails WCAG AA) — those links use `--ink-soft` at rest with `--gold-deep` only on hover, avoiding a legibility regression while still bringing in the brand accent. Verified: `npm run build` clean, live reload of `/login`, `/register`, `/forgot-password`, and `/terms` at 1280px and 375px — dark logo chip, gold button/checkbox/focus-ring, and consistent link styling confirmed on every page checked; zero console errors; `php artisan test` 86/87 passing (same single pre-existing unrelated failure noted in 1.2.3, untouched by this pass). |
| 1.2.3 | 2026-08-06 | Platform-loop pass | Fixed the hero photo on `welcome.blade.php` actually being invisible: the 1.2.2 pass had left a fully opaque `bg-[var(--charcoal)]` div stacked on top of an already-dimmed (`opacity-40`) photo, which together completely hid it — every viewer just saw flat charcoal. Removed the opaque layer, restored the photo to full strength, and replaced the single radial-gradient overlay with a graduated radial (dark at the text-bearing centre, fading toward transparent at the edges/corners) plus a subtle top/bottom linear scrim, matching the "full-strength photo + graduated gradient" pattern already used on Dot.Central and Dot.Agents' heroes — the photo is now genuinely visible while the headline/paragraph stay well above WCAG AA contrast. Separately, reviewed the actual content of the live `infodot.co.za` landing page (get_page_text + DOM inspection, not screenshots alone) and pulled in two authentic, non-cosmetic improvements: (1) the live site's hero centres on a functional "Ask: How To Register A Business?" search bar — that exact component already exists in this repo (`App\Livewire\Search`, used today in the authenticated nav and on `search_results.blade.php`) but had never been placed on the public welcome page; rather than embed the shared Livewire component (its hardcoded dark-navy palette belongs to the still-unrestyled authenticated "Workspace Explorer" nav, out of scope here), built a plain GET form on the welcome page styled to the gold/charcoal brand and posting to the same real `solution_search_results` route/controller. (2) While wiring it up, discovered `PagesController::solution_search_results()`'s non-sqlite branch used MySQL's `MATCH(...) AGAINST(?)` syntax unconditionally — invalid SQL on PostgreSQL, which is what this app (and every Dot Ecosystem platform) actually runs on, and confirmed via a live 500 (`SQLSTATE[42601]: syntax error ... AGAINST`) the moment the new hero search bar was tested end-to-end. The FULLTEXT index the MySQL branch depends on is only ever created when the migration driver is `mysql` (`create_solutions_table.php`/`create_questions_table.php`), so this branch had no working index on Postgres either — search across Questions/Solutions was silently broken for the platform's actual production database, not just in the new hero form. Added a dedicated `pgsql` branch using `ILIKE` (Postgres's native case-insensitive LIKE), mirroring the existing sqlite branch; kept the MySQL branch untouched for that driver. Verified live: the hero search bar now returns a real `200 OK` from `/solution-results?search=...` against the local Postgres dev database (previously a 500). Also lightly reworded the "Ecosystem Directory" card ("verified, first-party Dot app") and expanded the footer with a `Features` link (real route, previously missing) and the live site's own social links (Facebook, X, Instagram, LinkedIn — sourced directly from `infodot.co.za`'s own footer hrefs). No marketing copy was copied from the live site; only structure, the search-bar concept, and real account links were reused. Verified: `npm run build` clean, `php artisan test` 86 passed / 1 pre-existing unrelated failure (`AuthenticationFlowSmokeTest::test_auth_pages_render_the_existing_platform_designs` — asserts login page text `Sign in with credentials`, actual copy reads `Sign in with your credentials`; drift from an earlier auth-page pass, not touched by or related to this change), live reload at 1280px and 375px with photo visibility, search submission, and footer icons all checked directly in-browser. |
| 1.2.2 | 2026-08-06 | Platform-loop pass | Redesigned `welcome.blade.php` to visually match the structure of the live `infodot.co.za` site (dark persistent nav, full-bleed photographic hero with a large centered wordmark, three overlapping white capability cards, a two-column "story" section pairing an icon-led pitch with a photo + color-block CTA card, dark closing CTA and footer) while writing entirely original copy grounded in this repo's actual, wiki-documented capabilities — no marketing copy was copied from the live site. Swapped the old blue "inf" + gray circle/chevron mark (`img/logo.png`) for the new gold-circle mark supplied by the platform owner (`img/logo_white.png`, white "info" wordmark designed for dark backgrounds) across the nav, hero, closing CTA, and footer. `resources/views/vendor/jetstream/components/authentication-card-logo.blade.php` was deliberately left on the old `logo.png` — the guest auth layout (`layouts/guest.blade.php`) uses a light `--paper` background where the new mark's white text would be illegible; swapping it needs its own light-background lockup, out of scope for this pass. Two real bugs found and fixed along the way: (1) after bulk-replacing `text-[var(--x)]/NN` opacity-modifier classes with precomputed `text-[rgba(r,g,b,a)]` literals (Tailwind silently drops the arbitrary-value-plus-opacity-modifier-on-a-CSS-variable syntax — same class of bug hit on other platforms this rollout), the hero paragraph rendered invisible because `npm run build` hadn't been re-run after the class-name edit, so the JIT scanner had no compiled rule for the new class names even though the DOM was correct; fixed by rebuilding. (2) a pre-existing double-escaping bug in the `$features` PHP array — `'title' => 'Solutions &amp; how-to guides'` was being escaped a second time by Blade's `{{ }}`, rendering the literal text `Solutions &amp; how-to guides` instead of `Solutions & how-to guides`; fixed by un-escaping the source string. Verified: `npm run build` clean; live reload at 1280px and 375px viewports with hero, feature cards, story section, features grid, capabilities section, CTA, and footer all checked top-to-bottom; zero console errors; the ampersand fix confirmed via a page-text probe (`Solutions & how-to guides`, no `&amp;` anywhere on the page). |
| 1.2.1 | 2026-08-06 | Platform-loop pass | Fixed a visible `Deprecated: Constant PDO::MYSQL_ATTR_SSL_CA is deprecated since 8.5` banner rendering across the top of every page on PHP 8.5 — `config/database.php`'s stock, unused `mysql` connection block (this platform runs `pgsql`) referenced the old constant unconditionally; Laravel evaluates the whole config file regardless of which connection is active. Matched the fix already applied on 8 sibling platforms and just applied to Dot.Files: `(PHP_VERSION_ID >= 80500 ? Mysql::ATTR_SSL_CA : PDO::MYSQL_ATTR_SSL_CA)` with `use Pdo\Mysql;` added. Verified via `php -l` and a live reload with the banner confirmed gone and a clean console — PHP does run in this environment (contrary to an earlier pass's note that no PHP runtime was available; that appears to have been specific to that session, not a standing environment limitation). |
| 1.2.0 | 2026-08-06 | Platform-loop pass | Redesigned the guest-facing auth page family (login, register, forgot-password, reset-password, confirm-password, two-factor-challenge, verify-email, terms, policy) to match the `--paper`/`--ink`/`--blue`/`--silver` palette and `Baloo 2`/`DM Sans`/`Space Mono` type pairing established in `welcome.blade.php` (1.1.0). This app publishes Jetstream's views under `resources/views/vendor/jetstream/components/` (the `x-jet-*` aliases), so `authentication-card.blade.php`, `authentication-card-logo.blade.php`, `label.blade.php`, `input.blade.php`, `button.blade.php`, `checkbox.blade.php`, and `validation-errors.blade.php` there were restyled to the brand tokens rather than the app-level `resources/views/components/` used by most sibling platforms. Two real bugs found and fixed along the way: (1) `authentication-card-logo.blade.php` pointed at a broken relative path (`./img/logo_white.png`, a file resolved against the current route, not `asset()`) with no size classes at all — replaced with `{{ asset('img/logo.png') }}` at `h-16 sm:h-20`, the same nav-logo sizing already proven legible on the welcome page. (2) the shared `button.blade.php` had **no default class at all** (`$attributes->merge(['type' => 'submit'])`, nothing else) — every `x-jet-button` call site that didn't pass its own classes (forgot-password, reset-password, verify-email, confirm-password, two-factor-challenge) was rendering a completely unstyled bare HTML button; added a full brand-token default (`bg-[var(--blue)]` / `font-display` / `rounded-lg`, etc.). `login.blade.php` and `register.blade.php` were carrying a large, unrelated Creative Tim "Argon Design System" template fragment — a fixed dark nav with fake Facebook/Twitter/Instagram links and a `bg-gray-900` footer reading "Copyright © 2019 ... creative-tim.com" with a link to `creativetimofficial/argon-design-system`'s license — plus per-field inline overrides (`bg-white text-gray-700 uppercase ...`) duplicating/fighting the shared component defaults; removed both (visual chrome only, no route/form logic) and let the fields inherit the now-branded shared components. Swept the remaining auth pages' hardcoded `text-gray-600`/`hover:text-gray-900` for the token equivalents. `terms.blade.php` (a live route, `GET /terms` → `PagesController@terms`, though the view itself only has a placeholder "Terms & Conditions" heading with no body copy yet) and `policy.blade.php` (not currently routed — no `/policy` route exists) both got the same frame treatment (paper background, white card, `prose-invert`-equivalent light prose tokens) for consistency; no copy was invented for either. No `route()`/`action`/`@csrf`/input `name`/validation-display changes anywhere. Verified: `npm run build` clean; created and migrated a fresh local `infodot_pilot` Postgres database (didn't exist yet) and ran `php artisan serve`; `/login`, `/register`, `/forgot-password`, `/reset-password/{token}`, and `/terms` all render correctly with legible text and properly styled buttons at both 1600px and 375px, no horizontal overflow. A pre-existing, unrelated `PDO::MYSQL_ATTR_SSL_CA` deprecation notice (PHP 8.5 vs. `config/database.php` line 62) prints at the top of every DB-backed page in this local environment — not introduced by this change, out of scope, flagged separately. `confirm-password` and `two-factor-challenge` are gated behind an authenticated/partial-login session and were verified by code review only, reusing the same already-verified shared components. |
| 1.0.0 | 2026-08-04 | Platform-loop pass | Initial wiki. Architecture pass matching the Dot.Finance/Dot.Notify pattern (see Dot.Notify commit `e671436`, Dot.Finance commit `2f75bdb`): added `App\Models\Concerns\HasTeamScope`, an Eloquent global scope trait applied to `Obj`, `File`, and `Folder` (the only domain models genuinely owning a `team_id` column), scoping every query on those models to `Auth::user()->currentTeam->id` automatically. Deliberately **not** applied to `Questions`/`Solutions`/`Steps`/`Comment`/`Like`/`Associates` — real audit found these are genuinely global, publicly-browsed community content keyed by `user_id` for authorship display, not tenant isolation (see §3 for the full per-model reasoning, including why `Associates`' bidirectional follow-edge semantics rule it out too). No controller changes were needed — no controller currently queries the storage-layer models. Added `tests/Feature/InfoDotTeamScopeTest.php::test_scope_alone_blocks_cross_team_access_even_without_an_explicit_where`. No 403→404 assertion changes were needed: no Policy layer gates these models, and none were route-model-bound. Full suite re-run against real PostgreSQL after all changes: 93 deprecated (pre-existing, unrelated PHP 8.5 PDO constant notice) + 1 passed, 0 failed (up from 92/1/0 — the one new test). `vendor/bin/phpstan analyse --memory-limit=1G` (config already present from an earlier pass) ran successfully this time and reported no errors. `composer audit` found 6 pre-existing `guzzlehttp/guzzle` advisories (1 high, 5 medium); fixed via `composer update guzzlehttp/guzzle guzzlehttp/psr7 guzzlehttp/promises --with-all-dependencies`; audit clean afterward, full suite reconfirmed green. |
| 1.1.0 | 2026-08-06 | Platform-loop pass | Redesigned `resources/views/welcome.blade.php` following the Dot Ecosystem's guest-page pilot pattern set by Dot.Mines commit `d191d10` (+ its follow-up logo-sizing fix `dfc4547`) — four combined design skills (`frontend-design`, `design-taste-frontend-v1`, `emil-design-eng`, `ui-ux-pro-max`). The prior page was InfoDot's original pre-Jetstream-5 template: Tailwind v1 CDN import, Font Awesome CDN, a generic blue "hero + 3-equal-card" SaaS layout with filler copy ("Building Not Just A Community, But A Way To Successful Businesses") and stock Unsplash imagery unrelated to the actual product. Palette was sampled directly from `public/img/logo.png` (read as an image, pixel-sampled with PIL rather than guessed): `--blue #2487d4` (the "info" wordmark) and `--silver #bcbec0` (the "dot." circle and forward chevron), set against `--paper #f6f7f9`/`--ink #16212c` — a light, cool-toned system, deliberately not a dark near-black theme, because the real logo is legible on white and reads as a friendly, approachable community/identity brand rather than an industrial one (the opposite direction from Dot.Mines' warm dark gold/umber, by design — grounded in this platform's own mark, not copied from the pilot). Typography: `Baloo 2` (display — echoes the logo's own chunky rounded terminals) + `DM Sans` (body — clean and legible for Q&A/Solutions reading content) + `Space Mono` (tags/labels) — a different pairing from Dot.Mines' Outfit/Plus Jakarta Sans/JetBrains Mono, per the anti-slop constraint against reusing the pilot's literal type choices. Layout: asymmetric hero (left-aligned copy over a network-cabling photo already used as InfoDot's own hero image, credited to Taylor Vick), a divided single-column feature list (not a 3-card grid) covering the platform's real entities — SSO handoff tokens, Questions, Solutions/Steps, threaded Comments/Likes, Associates, Jetstream Teams — and an asymmetric two-column "how it connects" capabilities section grounded in §2's real architecture facts (Livewire 3, Sanctum one-time 5-minute tokens, Scout full-text search, shared PostgreSQL schema). Signature element: a large, quiet (16% opacity) line-art echo of the real logo's own icon geometry — an outlined circle, an offset accent dot, and the forward chevron — replacing Dot.Mines' headframe silhouette with InfoDot's own mark instead of reusing it. Copy rewritten throughout to remove the prior page's filler/hype language and fabricated framing in favor of concrete claims traceable to wiki §1/§3 (no invented stats, no dead `href="#"` links). Motion: single restrained scroll-reveal (`IntersectionObserver`, `prefers-reduced-motion` respected) plus `scale(0.97)` press feedback — no perpetual animation. **Logo legibility** (the explicit lesson from Dot.Mines' `dfc4547` follow-up): sized the nav logo `h-16 sm:h-20` (64px/80px) and footer `h-11` (44px) from the start, matching the exact sizes Dot.Mines landed on after its own reviewer feedback, rather than repeating the too-small-then-fixed cycle; confirmed via `getBoundingClientRect()` at 375px, 500px, and 1600px viewports that the rendered logo is exactly that size and the wordmark/circle/chevron are all legible at each. **Build tooling finding, fixed as a prerequisite**: `postcss.config.js` did not exist in this repo, so Vite was never actually running Tailwind through PostCSS — `resources/css/app.css` compiled to its 57-byte literal `@tailwind` directives with zero utility classes generated, meaning no Tailwind-based page in this app (not just this one) could have rendered styled in production. Added `postcss.config.js` (`tailwindcss` + `autoprefixer`) and `autoprefixer` as a devDependency; `npm run build` now emits a real ~81KB compiled stylesheet. **Verified**: `npm run build` clean. No PHP runtime exists in this sandbox (confirmed via `which php`, consistent with `CLAUDE.md`'s existing note that this environment has never had one), so instead of `php artisan serve`, the compiled `public/build` assets were served over a plain local HTTP static file server and opened in a real browser — same compiled CSS/JS the Laravel app itself serves, just without the PHP templating layer, with all `route()`/`@auth` output substituted by hand for the equivalent guest-state markup. Confirmed at 375×812, 500×700, and 1600×900: no horizontal overflow (`scrollWidth === clientWidth` at each), nav/footer logo both legible and correctly sized, hero/features/capabilities/CTA/footer all render cleanly, no console errors. |
